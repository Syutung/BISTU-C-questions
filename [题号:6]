//假设一堆由1分、2分、5分组成的n个硬币总面值为m分，求一共有多少种可能的组合方式（某种面值的硬币可以数量可以为0）。
//输入格式
//输入数据第一行有一个正整数T，表示有T组测试数据。接下来的T行，每行有两个数n，m，n和m的含义同上。
//输出
//对于每组测试数据，请输出可能的组合方式数，每组输出占一行。

//假设一堆由1分、2分、5分组成的n个硬币总面值为m分，求一共有多少种可能的组合方式（某种面值的硬币可以数量可以为0）。
//输入格式
//输入数据第一行有一个正整数T，表示有T组测试数据。接下来的T行，每行有两个数n，m，n和m的含义同上。
//输出
//对于每组测试数据，请输出可能的组合方式数，每组输出占一行。

//假设一堆由1分、2分、5分组成的n个硬币总面值为m分，求一共有多少种可能的组合方式（某种面值的硬币可以数量可以为0）。
//输入格式
//输入数据第一行有一个正整数T，表示有T组测试数据。接下来的T行，每行有两个数n，m，n和m的含义同上。
//输出
//对于每组测试数据，请输出可能的组合方式数，每组输出占一行。

#include"stdio.h"
int main()
{
    int a,m,n,k,o;
    printf("Please input test times: ");
    scanf("%d",&a);
    printf("Output:\n");
    for (int i = 0; i < a ; i++)
    {
        printf("Input Date: ");
        scanf("%d%d",&n,&m);
        k=0;
        for (int j = 0; j*5 <= m; j++)
        {
            for (int l = 0; l*2+j*5 <= m; l++)
            {
                o=m-2*l-5*j;
                if ((l+o+j)==n)
                {
                    k++;
                }
            }
        }
        printf("The total is:%d\n",k);

    }
    return 0;
}
